/** 判断数组是否包含某字符串（供 wxml 勾选态） */
function has(arr, code) {
  if (!arr || !arr.length || !code) return false;
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === code) return true;
  }
  return false;
}
module.exports.has = has;

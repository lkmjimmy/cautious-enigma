# 复制为 .env 或在 shell 中 export（不要提交真实密钥）

# 微信公众平台 → 开发 → 开发设置 → AppID / AppSecret
WECHAT_APP_ID=
WECHAT_APP_SECRET=

# 可选：管理员小程序用户的 openid，逗号分隔（生产环境登录后中台角色以此为准）
# 获取方式：先配置上一行并登录一次，看 GET /api/v1/me 返回的 openid，或查 db.json users
WECHAT_ADMIN_OPENIDS=

# 端口（云托管 / 容器会注入 PORT）
# PORT=3000
